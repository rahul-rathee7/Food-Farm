import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains:['upload.wikimedia.org','imgs.search.brave.com','images.unsplash.com','res.cloudinary.com','images.pexels.com','media.istockphoto.com','images.pexels.com','images.unsplash.com'],
  }
};

export default nextConfig;
